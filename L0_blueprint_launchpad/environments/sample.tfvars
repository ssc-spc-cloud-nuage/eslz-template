prefix = "ScDc-CIO_ESLZ" # This prefix need to be formatted as "${var.env}-${var.group}_${var.project}" like specified in the L1 tfvars file variables
convention = "passthrough"
enable_collaboration = true
location = "canadacentral"